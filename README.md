# MEXC Fair Price Spread Bot

Бот для мониторинга спреда между Fair Price и Last Price на фьючерсах MEXC (USDT-M) с **автоматической торговлей**.

## Описание

Бот отслеживает все USDT-M фьючерсные пары на бирже MEXC в реальном времени через WebSocket и:

1. Отправляет уведомления в Telegram при обнаружении спреда >= 7%
2. **Автоматически открывает позиции** на обнаруженных спредах
3. **Автоматически закрывает позиции** когда спред сужается до 2%

### Стратегия

- **🟢 LONG**: Fair Price > Last Price на >= 7% (рынок недооценен, ожидаем рост)
- **🔴 SHORT**: Fair Price < Last Price на >= 7% (рынок переоценен, ожидаем снижение)

### Параметры торговли

- Размер позиции: **$10 с плечом 20x** = $200 объема
- Максимум одновременно: **3 позиции**
- Выход из сделки: когда спред сокращается до **≤ 2%**

## Установка

1. Установите Python 3.8 или выше

2. Установите зависимости:

```bash
pip install -r requirements.txt
```

## Настройка

### 1. Telegram

Откройте `config.yaml` и настройте Telegram:

```yaml
telegram:
  bot_token: "ваш_токен_бота"
  chat_id: ваш_chat_id
```

### 2. MEXC API (для торговли)

Получите API ключи на MEXC:

1. Войдите на [MEXC](https://www.mexc.com/)
2. Перейдите в API Management
3. Создайте новый API ключ с правами на **торговлю фьючерсами**
4. **Важно:** Включите IP whitelist для безопасности

Добавьте ключи в `config.yaml`:

```yaml
mexc:
  api_key: "ваш_api_key"
  api_secret: "ваш_api_secret"

trading:
  enabled: true  # Включить автоматическую торговлю
```

### 3. Параметры торговли

Настройте под себя в `config.yaml`:

```yaml
trading:
  enabled: true
  position_size_usd: 10  # Размер позиции в USD
  leverage: 20  # Плечо
  max_positions: 3  # Максимум одновременных позиций
  exit_spread_threshold: 2.0  # Закрывать при спреде ≤ 2%

spread:
  min_threshold: 7.0  # Минимальный спред для входа
```

## Запуск

### Режим мониторинга (без торговли)

```bash
# В config.yaml установите trading.enabled: false
python mexc_futures_spread_bot.py
```

### Режим автоторговли

```bash
# В config.yaml установите trading.enabled: true и добавьте API ключи
python mexc_futures_spread_bot.py
```

### На сервере (VPS)

1. Установите screen:

```bash
# Ubuntu/Debian
sudo apt-get install screen
```

1. Запустите в screen:

```bash
screen -S mexc_spread_bot
cd /path/to/MONEY
python3 mexc_futures_spread_bot.py
```

1. Отключитесь от сессии (Ctrl+A, затем D)

2. Вернуться к сессии:

```bash
screen -r mexc_spread_bot
```

## Формат уведомлений

### Обнаружен спред

```
LISA_USDT 25.10%

Fair price: 0.050600
Last price: 0.037900

Side: 🟢 long
```

### Открыта позиция

```
📈 Opened LONG position on LISA_USDT
```

### Закрыта позиция

```
📉 Closed position on LISA_USDT (spread: 1.8%)
```

## Безопасность

> [!WARNING]
> **ВАЖНО:** Автоматическая торговля несет риски!
>
> - Начните с **малых сумм** для тестирования
> - Используйте **IP Whitelist** для API ключей
> - Установите **max_positions: 3** для ограничения риска
> - Мониторьте бота регулярно
> - Не храните большие суммы на фьючерсном аккаунте

## Технические детали

- **WebSocket**: Данные в реальном времени для максимальной скорости
- **Нет cooldown**: Все сигналы обрабатываются моментально
- **Автоматическое переподключение**: При разрыве соединения
- **Управление рисками**: Лимит на количество позиций
- **Логирование**: Все события в `bot.log`

## Файлы проекта

- `mexc_futures_spread_bot.py` - Основной файл бота
- `mexc_client.py` - Клиент MEXC API
- `spread_detector.py` - Логика обнаружения спредов
- `telegram_notifier.py` - Уведомления Telegram
- `trading_manager.py` - **Управление торговлей**
- `config.yaml` - Конфигурация
- `requirements.txt` - Зависимости

## Решение проблем

### Ошибка API ключей

- Проверьте правильность API key и secret
- Убедитесь, что ключ имеет права на торговлю фьючерсами
- Проверьте IP whitelist

### Позиции не открываются

- Проверьте, что `trading.enabled: true`
- Убедитесь, что достигнут порог спреда (>= 7%)
- Проверьте, не достигнут ли лимит max_positions
- Смотрите логи для деталей

### Позиции не закрываются

- Бот закрывает когда спред ≤ 2%
- Проверьте текущий спред на паре
- Смотрите логи

## Мониторинг

Логи сохраняются в `bot.log`:

```bash
tail -f bot.log
```

## Контакты

Если возникли вопросы или нужна помощь, пишите в Telegram!
